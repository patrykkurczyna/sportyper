spring:
  application:
    name: sportyper
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/sportyper?useTimezone=true&serverTimezone=UTC
    username: sportyper
    password: sportyper2017
    validation-query: SELECT 1
    test-on-borrow: true
    test-on-return: true

server:
  port: 8443
  ssl:
    enabled: true
    key-alias: spring
    key-store: src/main/resources/saml/keystore.jks
    key-store-password: secret

security:
  saml2:
    metadata-url: https://dev-277351.oktapreview.com/app/exkajnq03xCH4MJUk0h7/sso/saml/metadata
management:
  port: 8133